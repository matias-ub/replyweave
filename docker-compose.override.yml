services:
  backend:
    volumes:
      - ./backend:/app/backend
      - ./alembic.ini:/app/alembic.ini
    command:
      ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
